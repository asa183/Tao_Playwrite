# TAO 環境に関する環境変数のサンプルです。
# 実行前に、以下の値を実環境に合わせて設定し、シェルで `export` するか .env を読み込む仕組みを用意してください（本プロジェクトではdotenvは使用しません）。

# TAO のベースURL（ホスト or 任意の管理画面URL）
# 例: 管理画面の任意ページ（保護ルート可: テスト側でログインURLへ自動フォールバックします）
TAO_BASE_URL=https://staging.example.tao/ja/admin/sign_in

# 管理者メールアドレス（ログインID）
# 例: 管理者ユーザー
TAO_ADMIN_EMAIL=admin@example.com

# 管理者パスワード
# 例: 実際のパスワードを設定
TAO_ADMIN_PASSWORD=********

# （任意）ステージング環境などでHTTP Basic認証が有効な場合は設定してください。
# 例: ベーシック認証のユーザー/パスワード
# TAO_BASIC_AUTH_USER=stg-user
# TAO_BASIC_AUTH_PASSWORD=stg-pass
